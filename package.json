{
  "name": "helix",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:e2e": "playwright test",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@tauri-apps/cli": "^2",
    "prettier": "3.9.6"
  }
}
